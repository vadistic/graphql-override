schemaPath: src/schema.graphql
extensions:
  endpoints:
    default: http://localhost:4000
  override:
    - schema: tests/fixtures/schema.graphql
      overrides: tests/fixtures/overrides.graphql
      output: tests/temp/single-schema.generated.graphql
