# import * from '../src/directives.graphql'

directive @test on FIELD | FIELD_DEFINITION

type User {
  id: ID!
  workspace: String! @update @test
  password: String!
  isReplaced: Boolean
}

type Created @update {
  id: ID!
  workspace: String! @update
  password: String!
}
